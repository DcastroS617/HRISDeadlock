// Generated by Selenium IDE
using DOLE.HRIS.NUnit.Test;
using DOLE.HRIS.NUnit.Test.Utilities;
using NUnit.Framework;
using NUnit.Framework.Interfaces;
using OpenQA.Selenium;
using OpenQA.Selenium.Edge;
using OpenQA.Selenium.Interactions;
using OpenQA.Selenium.Support.UI;
using SeleniumExtras.WaitHelpers;
using Serilog;
using Serilog.Core;
using System;
using System.Collections.Generic;
using System.Configuration;
using System.IO;
using System.Net;
using System.Net.Http;
using System.Net.NetworkInformation;
using System.Threading;
using System.Threading.Tasks;

namespace DOLE.HRIS.Application.Automation.Selenium
{
    [TestFixture]
    [Category("Propositos")]
    public class TC001EliminacionPropositoEntrenamientoTest
    {
        private readonly string urlEntorno = Environment.GetEnvironmentVariable("UrlApplicationForTesting", EnvironmentVariableTarget.Process);
        private string urlTest = "";
        private readonly List<SeleniumEntity> seleniumEntities = new List<SeleniumEntity>();
        private readonly string urlScreen = "/Training/Maintenances/TrainingPurpose.aspx";


        private IWebDriver driver;
        public IDictionary<string, object> vars { get; private set; }
        private IJavaScriptExecutor js;
        WebDriverWait wait;
        Commons myCommons;
        String expectedErrorMessage = "No existen registros que mostrar.";


        [SetUp]
        public void SetUp()
        {
            urlTest = !string.IsNullOrEmpty(urlEntorno) ? urlEntorno : ConfigurationManager.AppSettings.Get("UrlApplicationForTesting");
            // Inicializa el driver de Edge
            driver = new EdgeDriver();
            myCommons = new Commons();
            myCommons.log();

            wait = new WebDriverWait(driver, TimeSpan.FromSeconds(90));
            driver.Manage().Window.Maximize();

            // Inicializa el ejecutor de JavaScript y el diccionario de variables
            js = (IJavaScriptExecutor)driver;
            vars = new Dictionary<string, object>();

            seleniumEntities.Add(new SeleniumEntity("PE01", "PE01_PropositoAEliminar"));
            seleniumEntities.Add(new SeleniumEntity("PE02", "PE02_PropositoAEliminar"));
        }

        [TearDown]
        protected void TearDown()
        {
            if (TestContext.CurrentContext.Result.Outcome.Status == TestStatus.Failed)
            {
                myCommons.TakeScreenshot(driver, TestContext.CurrentContext.Test.Name);
            }

            driver.Quit();
        }

        [Test]
        public async Task TC001EliminacionPropositoEntrenamiento()
        {
            await myCommons.SeleniumSetup(wait, driver, urlTest, urlScreen);

            for (int i = 0; i < 2; i++)
            {
                Thread.Sleep(1000);
                wait.Until(ExpectedConditions.ElementIsVisible(By.Id("ctl00_cntBody_btnAdd")));
                wait.Until(ExpectedConditions.ElementToBeClickable(By.Id("ctl00_cntBody_btnAdd"))).Click();
                myCommons.Log.Information($"{DateTime.Now}: Ingresando a la modal de Creación del Propósito.");

                myCommons.AgregarPropositos(wait, driver, js, i, seleniumEntities);
                myCommons.Log.Information($"{DateTime.Now}: Registro creado." + seleniumEntities[i].Code + seleniumEntities[i].Description);

                myCommons.BuscarPropositoPorCodigo(wait, driver, js, seleniumEntities[i].Code);
                Thread.Sleep(1000);
                wait.Until(ExpectedConditions.ElementIsVisible(By.Id("dvTrainingProgramCode")));

                myCommons.DeletePurpose(wait, driver, js, seleniumEntities[i].Code);

                wait.Until(ExpectedConditions.ElementIsVisible(By.Id("ctl00_cntBody_grvList")));

                string errorMessage = driver.FindElement(By.Id("ctl00_cntBody_grvList")).Text;
                Assert.AreEqual(expectedErrorMessage, errorMessage, "Error Message Incorrecto.");
                myCommons.Log.Information($"{DateTime.Now}: Validando el Assert {expectedErrorMessage} ");




            }
        }
    }
}